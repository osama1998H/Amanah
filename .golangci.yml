version: "2"

run:
  timeout: 5m

linters:
  default: standard
  disable:
    - errcheck

linters-settings:
  staticcheck:
    checks:
      - all
      - -SA1029

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
