# Amanah Payment Gateway - GolangCI-Lint Configuration
version: "2"

run:
  timeout: 5m

linters:
  default: standard
  disable:
    - errcheck

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
